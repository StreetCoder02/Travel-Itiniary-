# Copy this file to `.env` and provide values for your local setup.

# Optional: used only if you enable live Perplexity API calls.
PERPLEXITY_API_KEY=

# Optional: used for Google geocoding and places lookups.
GOOGLE_MAPS_API_KEY=

# Optional: message returned by /api/ping endpoint.
PING_MESSAGE=ping
